# AI Agent is an action layer

The AI Agent is available globally and within every Channel rather than existing as another Channel Entity. Each prompt receives an explicit current Channel, Channel References, schemas, and tool contracts but no stored Channel Record values. Resulting Operations preserve actor, origin, plan, and result. A dedicated Agent Conversation Channel may be added later without making conversational presentation the authority for agent work.
