# Each Datagram Service owns an identity realm

Each Datagram Service owns its people and authentication identities. A Local Service bootstraps one owner automatically; a Server Service adds people through invitations. Global Datagram accounts, cross-Service identity, and federation are outside the first release. This keeps local use independent and prevents an external identity service from becoming mandatory for self-hosting.
