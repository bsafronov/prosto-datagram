# Channel-backed entities use flat navigation

Every primary Channel Entity is associated with a Channel that supplies its collaboration identity, title, icon, permissions, activity, notifications, and navigation. Channels remain peers in one Flat Channel List, and meaningful Channel Activity can change their recency regardless of entity type. This deliberately gives conversations and structured data the same attention model instead of separating them into feature-specific navigation or a nested workspace tree.
