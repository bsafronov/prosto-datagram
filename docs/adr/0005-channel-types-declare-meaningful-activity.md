# Channel Types declare meaningful activity

Channel recency and attention are driven by meaningful Channel Activity declared by each Channel Type, not by every low-level mutation. A bulk import can create one Activity, while background recalculation can create none; person-specific notification policy remains separate. This keeps the Flat Channel List useful across high-volume structured data and conversations instead of letting implementation-level changes create attention noise.
