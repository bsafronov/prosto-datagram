# Record Reference Fields target one Channel

A Record Reference Field selects one target Channel, declares `one` or `many` cardinality, and stores stable Channel Record identities. Reverse references are derived rather than independently written. This supports relational data across flat Channels while avoiding copied values, ambiguous target types, and synchronization between two stored links.
