# Files are Channel-scoped Assets

Files are Assets governed by the permissions of their referencing Channel rather than independent Channels or public blob URLs. Messages may reference Assets, and a File Field may be added after the first Table–Chart slice. Local bootstrap uses local file storage while Server Services may use an object-store adapter. Asset bytes and metadata remain outside AI Agent context under the zero-data boundary.
