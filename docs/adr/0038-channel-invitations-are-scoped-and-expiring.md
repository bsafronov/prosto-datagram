# Channel Invitations are scoped and expiring

A Channel Owner or Admin may invite a person to one Channel with one proposed Channel Role. The invitation expires and cannot become a reusable Service-wide access mechanism. Acceptance selects an existing member from the Service directory or creates a Service-local identity, then grants only the stated Channel membership. This keeps sharing direct without introducing Workspace membership.
