# Charts are live derived Channels

A Chart Channel stores a live selection, aggregation, and presentation definition rather than copied source data. The first slice supports one Table source with filtering, grouping, and aggregation. Ordinary source changes produce Activity in the source Table Channel only; a Chart moves in the Flat Channel List for definition changes or explicit threshold, insight, or scheduled-report events, avoiding duplicate attention for every source mutation.
