# Each Channel has exactly one Owner

Each Channel has exactly one Channel Owner with final authority. Ownership may be transferred, but the Owner cannot leave or remove their Service identity while Channels still depend on them. Deployment Operators cannot seize ownership or inspect Channel data. This avoids ambiguous final authority while making explicit transfer part of offboarding.
