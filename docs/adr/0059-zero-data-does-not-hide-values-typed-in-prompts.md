# Zero-data does not hide values typed in prompts

The zero-data boundary prevents Datagram tools from returning stored or derived values to AI Agent context. It cannot prevent the model from seeing values a person deliberately includes in their prompt, such as a new Record's name or price. Product language and privacy guidance must distinguish user-supplied prompt content from Store-retrieved data instead of promising that no business value can ever reach a model.
