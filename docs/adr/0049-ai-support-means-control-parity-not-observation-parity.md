# AI support means control parity, not observation parity

The AI Agent may initiate every Channel Action and Channel Query available to the requesting person, so anything supported by UI or CLI can also begin with a prompt. It does not receive stored or derived result values. Human-facing hosts render those values directly from Result Handles and Data Views. Product language must therefore promise prompt-driven control parity, not claim that the model can inspect or narrate private data.
