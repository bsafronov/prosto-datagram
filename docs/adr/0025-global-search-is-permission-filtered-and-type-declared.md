# Global Search is permission-filtered and type-declared

One Global Search spans every Channel a person may access. Each Channel Type declares which Channel and Channel Record Fields are searchable, and results never reveal inaccessible Channels or values. Human-facing UI and CLI clients may display matching values. AI Agent tools preserve the zero-data boundary by returning non-data execution status or Result Handles rather than stored or derived values.
