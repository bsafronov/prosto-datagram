# First-release notifications stay in product

The first release delivers notification state through Unread Activity and realtime client events. It does not require email, mobile push, or external notification infrastructure. Later delivery adapters may consume the same permission-filtered Channel Activity without changing activity, unread, or mute semantics.
