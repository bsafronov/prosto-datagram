# Ad-hoc Data Views store queries, not results

An ad-hoc Data View retains the permission-checked query definition that produced it rather than copying returned values into agent or presentation state. Reopening the Data View executes the query against current data. A person may explicitly save the definition as an appropriate durable View or Chart Channel. Result Handles remain transient execution capabilities rather than persisted datasets.
