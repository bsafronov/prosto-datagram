# First release requires authenticated Channel membership

The first release has no public links or anonymous viewers. A person must have a Service-local identity and a Channel Role before reading or changing a Channel. This gives permissions, activity, audit, Result Handles, and realtime delivery one consistent actor boundary; public publishing can later be designed as a separate capability rather than an exception in every query.
