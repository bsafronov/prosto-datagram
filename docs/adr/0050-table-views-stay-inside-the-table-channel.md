# Table Views stay inside the Table Channel

Named combinations of visible Fields, filters, sorting, and grouping are Table Views within one Table Channel. A View may be personal or shared and reuses the Table's permissions and Channel Activity. A separate derived Channel is created only when the result needs independent permissions, Activity, notifications, or another Channel Entity contract. This avoids filling the Flat Channel List with ordinary presentation variations.
