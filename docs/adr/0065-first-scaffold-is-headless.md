# First scaffold is headless

The first scaffold contains the Datagram Service, CLI, MCP Gateway, Codex skill, Store adapters, and semantic View Definition validation. It does not create a bespoke graphical UI. CLI, MCP, tests, and generic JSON output prove the shared Action, Query, Operation, permission, and presentation contracts before a host-specific renderer exists.
