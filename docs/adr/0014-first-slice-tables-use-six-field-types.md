# First-slice Tables use six Field types

The first product slice supports Text, Number, Boolean, Date/Time, Dictionary Field, and Record Reference Field values. Attachments, formulas, geolocation, rich text, and arbitrary JSON are deferred. This set proves typed structured data, shared vocabularies, and cross-Channel links without making the first runtime solve every future value representation.
