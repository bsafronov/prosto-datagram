# Unread state follows Channel Activity

Each person has a last-read Channel Activity position per Channel. Unread badges count meaningful Activities rather than changed Channel Records, so one bulk import can create one unread item instead of thousands. Muting affects notifications but does not erase Unread Activity, and opening a Channel marks the visible Activity range as read.
